/**
 * Enum representing the roles within the hospital management system.
 */ 
export enum LPHStaffRole {
  ADMIN = 'admin',          // Administrator role
  BACKSTORE = 'Backstore',  // Backstore role
  DENTAL = 'Dental',        // Dental department role
  FINANCE = 'Finance',      // Finance department role
  LAB = 'Lab',              // Lab department role
  MATERNITY = 'Maternity',  // Maternity department role (corrected typo)
  OPD = 'OPD',              // Outpatient department role
  PHARMACY = 'Pharmacy',    // Pharmacy department role (consistent capitalization)
  RECEPTION = 'Reception',  // Reception department role
  VITALS = 'Vitals',        // Vitals department role
  XRAY = 'X-Ray'            // X-Ray department role
}
